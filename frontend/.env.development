# Local dev: the Vite dev server (:5173) talks to the backend running on :8000.
# In the single-container production build this is left unset so requests are
# same-origin (relative paths).
VITE_API_BASE_URL=http://localhost:8000
